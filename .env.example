# ============================================
# CLBH Jeppson Law App - Environment Variables
# ============================================
#
# Copy this file to .env and update the values
# cp .env.example .env
#
# IMPORTANT: Never commit .env file to version control
#

# ============================================
# MongoDB Configuration
# ============================================

# MongoDB root username (default: admin)
MONGO_ROOT_USERNAME=admin

# MongoDB root password
# IMPORTANT: Change this in production!
MONGO_ROOT_PASSWORD=changeme

# Database name (default: clbh)
DB_NAME=clbh

# ============================================
# Backend Configuration
# ============================================

# Admin API key for protecting /api/admin/* endpoints
# IMPORTANT: Set a strong, unique key in production!
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_KEY=please-set-a-secure-admin-key

# CORS allowed origins (comma-separated)
# For multiple origins: http://localhost:3000,https://yourdomain.com
CORS_ORIGINS=http://localhost:3000

# ============================================
# Frontend Configuration
# ============================================

# Backend API URL
# Development: http://localhost:8000
# Production: https://api.yourdomain.com
REACT_APP_BACKEND_URL=http://localhost:8000

# ============================================
# Development Only
# ============================================

# Uncomment for development with hot-reloading
# CHOKIDAR_USEPOLLING=true
# WATCHPACK_POLLING=true

# ============================================
# Production Recommendations
# ============================================
#
# 1. Generate a secure ADMIN_KEY:
#    python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# 2. Use a strong MONGO_ROOT_PASSWORD:
#    python -c "import secrets; print(secrets.token_urlsafe(24))"
#
# 3. Update CORS_ORIGINS to your actual domain
#
# 4. Update REACT_APP_BACKEND_URL to your production API URL
#
# 5. Consider using Docker secrets for sensitive values
#
